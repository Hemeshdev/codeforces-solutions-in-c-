#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main() 
{
    int n,m;
    cin>>n>>m;
    int t=min(n,m);
    if(t%2==0) cout<<"Malvika"<<endl;
    else cout<<"Akshat"<<endl;
}
